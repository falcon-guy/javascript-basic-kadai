 // 変数の宣言(体重)
 let Bodyweight;

 // 変数の宣言(身長)
 let Height;

 // 値の代入(体重)
 Bodyweight = 68;

 // 値の代入(身長)
 Height = 1.7;
 
 // 変数の宣言(BMI)
 let Bmi;

 // 値の代入(身長)
 Bmi = Bodyweight / (Height*Height);
 
 
 // 体重と身長のBMI（体格指数）を出力
 console.log(Bmi);